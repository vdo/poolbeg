apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "meddler.fullname" . }}-config
  labels:
    {{- include "meddler.labels" . | nindent 4 }}
data:
  config.yaml: |
    {{- .Values.config | toYaml | nindent 4 }}
